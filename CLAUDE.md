# Flow Builder

After committing a user-facing feature or fix, add a new entry to the top of `HISTORY` in `docs/changelog.html` (plain language, no internal jargon) with the commit hash, date, and a tag. If the commit fulfills something listed in `ROADMAP` in that same file, remove that roadmap entry.
